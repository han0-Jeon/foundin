# 100건 배치 평가 리포트 — 2026-07 (최종)

실제 정부지원사업 공고 100건(K-Startup 37 · 중소벤처기업부 32 · 온통청년 22 · KOCCA 9, 활성 공고 층화 표본)을
전량 분석한 실측 결과입니다. 러너 `solar-open2`(베타) · 동시성 3 · 발행 없음(지표 전용).
측정 → 수정 → 재측정을 반복한 마지막 라운드 수치입니다.

## 결과 분포

| 결과 | 건수 | 의미 |
| --- | ---: | --- |
| 발행 (published) | **78** | 검증 게이트 통과, 브리프 생성 |
| 반려 (rejected) | 9 | 공고 아님 판정 — 전부 포털 메인·게시판 목록 페이지 (정확한 반려) |
| 보류 (held) | 8 | fail-closed: 마감일 원문 검증 실패 6 · 인용 통과율 미달 2 |
| 수집 실패 | 5 | 죽은 링크 3 · JS 렌더링 전용 SPA 2 |
| 추출 실패 | 0 | 초판의 3건(mss 장문)은 API 안정 시간대 재실행 결과 전부 '보류'로 확정 — 마감일 원문 미검증 |

**발행률: 표본 전체 78% · 유효 공고 기준 85.7%** (비공고 9건 제외 78/91).
남은 미발행 13건의 구성: 정당한 품질 보류 8(전부 마감일 원문 검증 실패·인용 통과율 미달),
원문 소멸·JS 전용 SPA 5. 코드로 회복 가능한 층은 소진됐고, 미발행 13건은 전부 fail-closed 가
정당하게 잡은 것이거나 원문 자체가 사라진 경우입니다. 초판에서 'API 불안정'으로 미처리였던
mss 장문 3건도 재실행 결과 발행이 아니라 보류(마감일 원문 미검증)로 확정됐습니다.

## 인용 검증 (발행 브리프 78건)

- 평균 인용 원문 대조 통과율 **92.0%**, 50% 미만 0건
- 검증 실패 인용의 항목은 발행물에서 "원문 확인 필요" 표기 + 기관 문의 질문으로 변환
- **환각 인용이 검증을 뚫고 발행된 사례: 0건**

## 발행 게이트 눈금 검증 (요건 60% · 전체 50%)

결론: **현행 유지.**

1. 50% 미만 구간 발행 시도가 0건 — 눈금 언저리에서 잘리는 브리프가 없습니다
2. 보류는 전부 개별 하드 룰(마감일 재파싱, 통과율)로 잡혔습니다 — 눈금을 낮춰도 구제되지 않습니다
3. 눈금을 80% 로 올리면 멀쩡한 브리프 11건이 떨어집니다

## 처리 성능

- 공고당 Solar 호출 2~3회 (공공 도메인은 프리체크가 판정을 생략)
- HWP/HWPX 첨부 파싱: 미분석 24건 중 HWP 계열 **0건** (자체 파서 전량 처리)
- 베타 기간 누적 약 568만 토큰, 비용 $0 (콘솔 실측)

## 측정 → 수정 → 재측정 루프가 잡은 것

같은 표본의 첫 라운드는 발행 26건이었습니다. 평가가 직접 찾아낸 수정 8건 후 78건:

1. K-Startup 마감 공고의 빈 셸 페이지 → ongoing↔deadline 형제 경로 폴백
2. 제출 URL 정규화가 상세 화면 파라미터(schM=view)를 지우던 결함
3. 위험신호 스캔이 첨부 신청서 양식의 표준 문구에 오탐 → 랜딩 문서 한정
4. 추론 모델 토큰 절단 응답이 파서로 흘러 실패 → 버퍼 증량 재시도
5. 장문 추출 타임아웃 180초 → 300초
6. 한국어 날짜 표기 재파싱 커버리지 확장 (굽은 따옴표 연도·두 자리 압축·요일 괄호)
7. 원문 링크 오염 보정 (&amp; 잔재·스킴 없는 주소) · 네이버 블로그 모바일 폴백
8. 장문 문서 강등 재시도 (타임아웃 시 입력 축소 후 1회 재시도)

> 지표 전용 리포트입니다. 이 평가는 DB 기록·발행을 하지 않았습니다.
> 재현: `npm run eval -- --urls eval-urls.txt` (URL 목록은 활성 공고에서 층화 추출)
